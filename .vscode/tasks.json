{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "build",
            "command": "dotnet",
            "type": "process",
            "args": [
                "build",
                "${workspaceFolder}/Ajax Notes.csproj"
            ],
            "problemMatcher": "$msCompile"
        }
    ]
}